import 'jest';

describe('Parser', () => {
    describe('validateFile', () => {
        it.todo('validates an account number file')
        it.todo('invalidates an invalid account number file')
    })

    describe('parseAccountNumbersFrom', () => {
        it.todo('parses an account number file with every digit')
    })
})