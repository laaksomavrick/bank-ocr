describe('Reconciler', () => {
    describe('reconcileAccountNumbers', () => {
        it.todo('throws when no buffer exists for an account number');
        it.todo('does not attempt to reconcile OK account numbers');
        it.todo(
            'can reconcile an unambiguous account number with an ERR state',
        );
        it.todo(
            'can reconcile an unambiguous account number with an ILL state',
        );
        it.todo('can reconcile an ambiguous account number with an ERR state');
        it.todo('can reconcile an ambiguous account number with an ILL state');
    });
});
